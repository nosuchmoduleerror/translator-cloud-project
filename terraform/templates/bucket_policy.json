{
  "Version": "2012-10-17",
  "Statement": [
    {
      "Sid": "owner_statement",
      "Effect": "Allow",
      "Principal": {
        "AWS": "${aws_principal}"
      },
      "Action": "${action}",
      "Resource": "${resource_arn}"
    }
  ]
}